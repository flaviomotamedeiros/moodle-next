JWT_SECRET=change-me-in-production
FRONTEND_URL=http://localhost:3000
PORT=4000

# New-system database (Prisma)
PRISMA_DATABASE_URL="file:./dev.db"

# Legacy connection — only needed to re-run the one-time migration script
# (npm run migrate:legacy). Not used at runtime.
LEGACY_DB_HOST=localhost
LEGACY_DB_PORT=3307
LEGACY_DB_USER=moodle
LEGACY_DB_PASSWORD=moodlepassword
LEGACY_DB_NAME=moodle
